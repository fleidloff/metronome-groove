export const CLAVES_NOMINAL_VELOCITY = 0.5

/** Silence before the attack, to -60 dB of peak: a buffer started at `t` is
 *  heard at `t + 0.0083`. */
export const CLAVES_LEAD_IN_S = 0.0083

export const CLAVES_SAMPLE_URL = '/samples/claves_mf.flac'
