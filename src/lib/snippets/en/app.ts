import type { AppSnippets } from '../types'

export const app = {
  name: 'Metronome',
  tagline: 'A metronome that plays grooves.',
  sampleCredit: 'Drum samples provided by DrumGizmo.org',
} satisfies AppSnippets
